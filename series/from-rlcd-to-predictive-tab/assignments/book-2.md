Author BOOK 2 ONLY: One Read, Many Decisions, slug one-read-many-decisions, four native Orly chapter scenes and matching Storybook stories. You are the explicitly requested Claude Fable 5.1 visualization author. The parent wrote narration; preserve every caption verbatim and in order from series/from-rlcd-to-predictive-tab/assignments/book-2-script.json. Do not author another book or delegate. Chapters: ordinary generation; shared prefill; batched permitted choices and code assembly; token collisions plus actual limitations.

Read AGENTS.md, .claude/commands/new-book.md, .claude/commands/new-series.md, .agents/skills/viz-scene/SKILL.md, generator/prompts/storyboard.txt, the series visualization-brief.md, and the sliced book-2-script.json. Inspect 2–3 existing exemplars (kalman, pagerank, differential-dataflow) and the now-verified next-useful-action chapters/stories. Reuse its shared/profile-page.tsx captionPlan and vocabulary when useful (read only). Read source engine_mlx.py, schema.py, prompt_builder.py, browser/app.js and PROJECT_CHARTER.md under /Users/blamy/Documents/Codex/2026-09-16/can-you-get-this-running-on (read only). Do not change source or narration.

Write ONLY apps/bookshelf/src/viz/books/one-read-many-decisions/**. Book-local helpers belong in a shared/ subdirectory, not top-level .tsx where the registry would count them as chapters. Create chapter-1.tsx through chapter-4.tsx, each exporting Render and vizScene; CSF3 chapter-N.stories.tsx using Player, title Books/One Read, Many Decisions/Chapter N. Notes may go in this book directory. Do not modify core, primitives, settings, library, dependencies, other books or public assets. Parent runs typecheck/build/caption extraction and browsers. No shell tools; focus on authoring a complete book.

Use one persistent transforming mechanism per supplied chapter, meaningful D3 scales/paths/interpolation/matrix work, cameraInterp for every sampled camera, pure deterministic time, and clean transitions. No slideshow panels or static boxes-only scenes. No audio or timers. Keep implementation compact: prefer roughly 250–400 lines per chapter plus small helpers, reuse components rather than elaborate incidental UI. Caption durations ~150 words/minute via captionPlan; no repetitive padding.

IMPORTANT LESSON FROM BOOK 1 REVIEW: The caption area is SCREEN y>=575, not merely world y>=630. After any Camera transform (screenY=360+k*(worldY-camY)), every active visual/label must stay above 575 and within x20..1260. Use modest earned camera motion; if zooming into a subregion, fade irrelevant layers to zero BEFORE they are clipped and reveal again only after pullback. No cut text or hidden labels. Closing text gets a cleared stage/opaque panel. Colour never carries meaning alone. Onscreen teaching labels should be readable (~15–22px), short, with room to breathe.

Facts: Native reference Qwen2.5 1.5B has known collision/cache/reporting defects. The current browser WebLLM chat demo is ordinary grammar-constrained generation, not faithful RLCD. The intended contract has one logical shared prefill, real batched independent suffixes, correct masks/positions/cache ownership, scored permitted choices, explicit continuation, and code-built output. Ordinary generation already caches. No calibrated-confidence/speedup/zero-copy/one-total-forward-pass claims. Scores are illustrative ranking scores. Synthetic paths [17], [17,42], [17,91] are SCHEMATIC token paths, not real Qwen tokenization; keep terminal branch separate, continue only unresolved paths. Use fictional support note 'The export failed twice; I need the report for tomorrow.' for independent topic/urgency; do not depict independent future browser actions. Make current/reference/proposed distinctions visible. Return complete authored files and brief mechanism notes. Parent will verify and send bounded corrections if needed.
