// The Long-Context Trade: honest fp16 memory arithmetic — 512 KiB of KV per
// token vs a flat 64 MiB state; 1024x smaller at 128k context.
export { Render, vizScene } from '../../explainers/ssm-context-trade/SsmContextTrade';
