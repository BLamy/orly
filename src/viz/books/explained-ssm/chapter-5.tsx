// Where This Sits: attention vs gated states vs the delta rule on a real
// rewrite test — the bridge to Explained: Transformers and the delta-rule book.
export { Render, vizScene } from '../../explainers/ssm-memory-spectrum/SsmMemorySpectrum';
