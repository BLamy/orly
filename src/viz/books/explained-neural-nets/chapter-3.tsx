// The Loss Landscape: where training lives — one point in parameter space is one fitted curve,
// and every training run is a walk across the rippled surface between them.
// Published from the catalog: src/viz/explainers/loss-landscape IS this chapter.
export { Render, vizScene } from '../../explainers/loss-landscape/LossLandscape';
