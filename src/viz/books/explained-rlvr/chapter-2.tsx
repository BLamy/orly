// The RLVR Loop: a toy softmax policy trained with REINFORCE against an
// executable verifier — pass/fail rewards, genuinely simulated (31.5% → 77.5%).
// Published from the catalog: src/viz/explainers/rlvr-loop IS this chapter.
export { Render, vizScene } from '../../explainers/rlvr-loop/RlvrLoop';
