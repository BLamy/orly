// GRPO at Toy Scale: the group-relative advantage actually computed on sampled
// completions, then a real 300-step GRPO training run (31.5% → 84.6%).
// Published from the catalog: src/viz/explainers/grpo-group IS this chapter.
export { Render, vizScene } from '../../explainers/grpo-group/GrpoGroup';
