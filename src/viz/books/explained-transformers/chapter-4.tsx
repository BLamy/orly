// The Residual Stream: a highway with editors — every block reads the stream and adds a small edit back.
// Published from the catalog: src/viz/explainers/residual-stream IS this chapter.
export { Render, vizScene } from '../../explainers/residual-stream/ResidualStream';
