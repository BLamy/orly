// Embeddings: words become directions — a toy vector space where distance is similarity and vector arithmetic carries meaning
// Published from the catalog: src/viz/explainers/word-embeddings IS this chapter.
export { Render, vizScene } from '../../explainers/word-embeddings/WordEmbeddings';
