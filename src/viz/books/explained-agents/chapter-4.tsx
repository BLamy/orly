// Why Agents Fail: 0.95^20 computed + Monte Carlo, poisoned observations,
// evaluator gaming (bridge to explained-alignment). From src/viz/explainers/agent-failure.
export { Render, vizScene } from '../../explainers/agent-failure/AgentFailure';
