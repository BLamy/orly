// Decision Trees: twenty questions with data — a real greedy split search on information gain.
// Published from the catalog: src/viz/explainers/decision-tree IS this chapter.
export { Render, vizScene } from '../../explainers/decision-tree/DecisionTree';
