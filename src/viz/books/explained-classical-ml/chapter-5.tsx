// Clustering: structure without labels — a recorded k-means run, its honest failure, and the mixture that softens it.
// Published from the catalog: src/viz/explainers/clustering IS this chapter.
export { Render, vizScene } from '../../explainers/clustering/Clustering';
