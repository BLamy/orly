// Support Vector Machines: the widest street — an exactly solved max margin and the kernel trick lifting XOR.
// Published from the catalog: src/viz/explainers/svm IS this chapter.
export { Render, vizScene } from '../../explainers/svm/Svm';
