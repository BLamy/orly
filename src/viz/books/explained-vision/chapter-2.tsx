// Pooling: keep what fires, forget exactly where — a real max pool where a one-pixel shift changes 10/100 map cells and 0/25 pooled cells
export { Render, vizScene } from '../../explainers/vision-pooling/VisionPooling';
