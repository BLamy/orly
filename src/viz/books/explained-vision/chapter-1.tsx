// Kernels: a convolution layer is a bank of learned detectors — a real 12×12 image convolved with real Sobel + texture kernels
export { Render, vizScene } from '../../explainers/vision-kernels/VisionKernels';
