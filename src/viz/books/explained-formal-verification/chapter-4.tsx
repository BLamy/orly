// A Judge You Cannot Charm — judges bend under pressure; the kernel's line stays at zero.
// Published from the catalog: src/viz/explainers/kernel-trust IS this chapter.
export { Render, vizScene } from '../../explainers/kernel-trust/KernelTrust';
