// Sparse Autoencoders: unmixing the residual stream — a real SAE recovering every planted direction.
// Published from the catalog: src/viz/explainers/sparse-autoencoder IS this chapter.
export { Render, vizScene } from '../../explainers/sparse-autoencoder/SparseAutoencoder';
