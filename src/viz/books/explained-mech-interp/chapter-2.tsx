// Superposition: more features than neurons — the toy model actually trained, pentagon geometry and measured interference.
// Published from the catalog: src/viz/explainers/superposition IS this chapter.
export { Render, vizScene } from '../../explainers/superposition/Superposition';
