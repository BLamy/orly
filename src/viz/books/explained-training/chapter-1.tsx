// Ill-Conditioning: why stretched valleys break gradient descent — the condition number is the slowdown factor, and optimizers are ways of coping with it.
// Published from the catalog: src/viz/explainers/conditioning IS this chapter.
export { Render, vizScene } from '../../explainers/conditioning/Conditioning';
