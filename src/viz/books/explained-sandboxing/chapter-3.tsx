// How We Caged the Agent — almostnode's real cross-origin iframe + service worker + postMessage architecture.
export { Render, vizScene } from '../../explainers/sandbox-architecture/SandboxArchitecture';
