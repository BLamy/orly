import type { Meta, StoryObj } from '@storybook/react-vite';
import { AlmostnodeWalls } from './AlmostnodeWalls';

const meta: Meta<typeof AlmostnodeWalls> = {
  title: 'Explainers/almostnode — Walls & Escape Hatches',
  component: AlmostnodeWalls,
};
export default meta;

export const WallsAndEscapeHatches: StoryObj<typeof AlmostnodeWalls> = {};
