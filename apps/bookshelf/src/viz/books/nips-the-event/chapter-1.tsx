// The Event (Nostr Implementation Possibilities №1), chapter 1.
// Published from src/viz/explainers/nostr-event-anatomy — the NIP-01 event:
// JSON → serialization array → REAL SHA-256 id → back into the id slot.
export { Render, vizScene } from '../../explainers/nostr-event-anatomy/NostrEventAnatomy';
