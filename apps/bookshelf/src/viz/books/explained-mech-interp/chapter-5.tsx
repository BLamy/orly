// Ablation: proof by removal — real knockouts, 9/9 collapsing to 0/9 while the untouched behavior survives.
// Published from the catalog: src/viz/explainers/ablation IS this chapter.
export { Render, vizScene } from '../../explainers/ablation/Ablation';
