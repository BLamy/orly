// The Doctrine: 14 genuinely simulated training runs sweeping verifier
// gameability — a phase-transition cliff at the honest pass rate; the bridge
// to run-time adversarial verification (the loop books), at gradient scale.
// Published from the catalog: src/viz/explainers/rlvr-doctrine IS this chapter.
export { Render, vizScene } from '../../explainers/rlvr-doctrine/RlvrDoctrine';
