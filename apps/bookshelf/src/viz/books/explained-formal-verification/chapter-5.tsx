// The Unnegotiable Verifier — the ladder of verifiers, closed at the kernel.
// Published from the catalog: src/viz/explainers/verification-limit IS this chapter.
export { Render, vizScene } from '../../explainers/verification-limit/VerificationLimit';
