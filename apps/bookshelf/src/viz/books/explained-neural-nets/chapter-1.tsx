// The Perceptron: a line that learns from its own mistakes — the real update rule, replayed.
// Published from the catalog: src/viz/explainers/perceptron IS this chapter.
export { Render, vizScene } from '../../explainers/perceptron/Perceptron';
