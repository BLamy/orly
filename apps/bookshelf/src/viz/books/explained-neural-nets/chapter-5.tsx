// Regularization: the price of complexity — ridge shrinks the weights, dropout averages an ensemble, and both trade a little training fit for generalization.
// Published from the catalog: src/viz/explainers/regularization IS this chapter.
export { Render, vizScene } from '../../explainers/regularization/Regularization';
