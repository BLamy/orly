// Selectivity: the copy task run for real — fixed gates end at recall 0.05,
// Mamba-style input-dependent gates at 1.00.
export { Render, vizScene } from '../../explainers/ssm-selectivity/SsmSelectivity';
