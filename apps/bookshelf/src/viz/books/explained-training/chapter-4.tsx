// Bias and Variance: thirty resampled training sets split expected error into squared bias, variance, and noise — verified numerically at a probe point.
// Published from the catalog: src/viz/explainers/bias-variance IS this chapter.
export { Render, vizScene } from '../../explainers/bias-variance/BiasVariance';
