// What Filtering Saves: the same collapse loop with a verifier veto — same
// seed, both runs measured; collapse averted (0.24 vs 0.96 at gen 30).
// Published from the catalog: src/viz/explainers/filtered-loop IS this chapter.
export { Render, vizScene } from '../../explainers/filtered-loop/FilteredLoop';
