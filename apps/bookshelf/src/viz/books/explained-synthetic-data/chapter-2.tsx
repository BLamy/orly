// Model Collapse: the loop actually iterated 30 generations (seed 25) —
// variance shrinking, mean wandering, tails dying, all measured.
// Published from the catalog: src/viz/explainers/model-collapse IS this chapter.
export { Render, vizScene } from '../../explainers/model-collapse/ModelCollapse';
