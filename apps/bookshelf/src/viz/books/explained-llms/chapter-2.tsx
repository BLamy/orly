// Next-token prediction as compression — a real bigram model, measured in bits.
// Published from the catalog: src/viz/explainers/next-token IS this chapter.
export { Render, vizScene } from '../../explainers/next-token/NextToken';
