// Occlusion: where does the network look? — the square detector really re-run 36 times behind a sliding gray patch
export { Render, vizScene } from '../../explainers/vision-occlusion/VisionOcclusion';
