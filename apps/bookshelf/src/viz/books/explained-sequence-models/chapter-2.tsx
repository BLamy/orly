// LSTM Gates: the real LSTM recurrence holding a bit through 17 noisy steps a plain RNN forgets.
// Published from the catalog: src/viz/explainers/lstm-gates IS this chapter.
export { Render, vizScene } from '../../explainers/lstm-gates/LstmGates';
