// Attention Sinks: the reported first-token mass pattern, and a toy showing
// why evicting sink tokens breaks streaming (measured 99% output swing).
// Published from the catalog: src/viz/explainers/attention-sinks IS this chapter.
export { Render, vizScene } from '../../explainers/attention-sinks/AttentionSinks';
