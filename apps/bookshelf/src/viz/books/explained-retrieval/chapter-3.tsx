// HNSW: a real 3-layer index and a really-run greedy descent — 21 comparisons instead of 60.
// Published from the catalog: src/viz/explainers/hnsw IS this chapter.
export { Render, vizScene } from '../../explainers/hnsw/Hnsw';
