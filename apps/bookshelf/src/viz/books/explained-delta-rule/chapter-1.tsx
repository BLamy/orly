// Fast Weights: a matrix as key-value memory — real outer-product writes and
// reads, measured capacity crowding.
export { Render, vizScene } from '../../explainers/delta-fast-weights/DeltaFastWeights';
