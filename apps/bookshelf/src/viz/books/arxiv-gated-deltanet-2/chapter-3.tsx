// Erase, then write — the paper's decoupled update rule (Eq. 10 vs Eq. 6),
// computed: the coupled clobber, the scalar trade-off frontier, and the
// decoupled point off it. src/viz/explainers/erase-write-decoupling.
export { Render, vizScene } from '../../explainers/erase-write-decoupling/EraseWriteDecoupling';
