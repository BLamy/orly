// Variance and Significance: 2000 seeded benchmark reruns show a one-point gain is noise at n=100.
// Published from the catalog: src/viz/explainers/eval-variance IS this chapter.
export { Render, vizScene } from '../../explainers/eval-variance/EvalVariance';
