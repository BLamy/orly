node_modules
.nx
dist
storybook-static
*storybook.log
*.local
.DS_Store
.env

# parody cover seed images (regenerable via `node generator/setup-seeds.mjs`)
generator/seeds/raw/
generator/seeds/clean/

# scratch screenshots in the repo root (cover art under public/ is kept)
/.playwright-mcp
/*.png
public/generated/*/animal.png

# rendered video exports + generated library dumps (regenerable)
exports/
public/generated/*.full

# Private authoring handoffs/results and regenerable Blender working files
.orly-work/
# Private source material used to ground the public explainer, retained locally.
series/from-rlcd-to-predictive-tab/source-snapshot/
artifacts/blender/

artifacts/three/
